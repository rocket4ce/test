<p id="notice"><%= notice %></p>

<h1>
  <%= @weather%>
</h1>
<p>
  <strong>name:</strong>
  <%= @course.name %>
</p>

<p>
  <strong>desciption:</strong>
  <%= @course.description %>
</p>

<p>
  <strong>City:</strong>
  <%= @course.city.name %>
</p>

<p>
  <strong>Start course:</strong>
  <%= @course.start_course %>
</p>

<ul>
  <% @course.sessions.each do |s| %>
    <li>
      <%= s.position_session %> - <%= s.start_session %>
    </li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
